package jsettlers.mapcreator.data;

import jsettlers.common.map.IMapData;

/**
 * 
 * @author michael
 *
 */
public class MapDataHasher {
	public static long computeHash(IMapData data) {
		long hash = 0;
		
		for (int x = 0; x < data.getWidth(); x++) {
			for (int y = 0; y < data.getHeight(); y++) {
				
			}
		}
		
		return 0;
	}
}

package jsettlers.graphics.startscreen.interfaces;

public interface IJoinableGame extends IMapDefinition  {
	/* TODO: Send a list of players / how many places are still free / ... Currently not needed.*/
}

package jsettlers.graphics.image;

/**
 * This is a shadow image.
 * @author michael
 *
 */
public class ShadowImage extends Image {

	/**
	 * Generates a shadow image. 
	 * @param data The data to use.
	 */
	public ShadowImage(ImageDataPrivider data) {
	    super(data);
    }
}
